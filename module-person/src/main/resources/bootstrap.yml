spring:
  cloud:
    config:
      name: config
      profile: dev
      discovery:
        enabled: true
        service-id: service-config
eureka.client.serviceUrl.defaultZone: http://localhost:8000/eureka/